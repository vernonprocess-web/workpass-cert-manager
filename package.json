{
  "name": "workpass-cert-manager",
  "version": "1.0.0",
  "description": "Work Permit & Certificate Manager — Cloudflare Workers + Pages + D1 + R2",
  "private": true,
  "scripts": {
    "dev:worker": "cd worker && wrangler dev --local --persist-to=../.wrangler/state",
    "dev:frontend": "npx -y serve frontend -l 8080",
    "deploy:worker": "cd worker && wrangler deploy",
    "deploy:frontend": "wrangler pages deploy frontend --project-name=workpass-cert-manager",
    "db:migrate": "wrangler d1 execute workpass-cert-db --file=./database/migrations/0001_initial.sql",
    "db:migrate:local": "wrangler d1 execute workpass-cert-db --file=./database/migrations/0001_initial.sql --local",
    "db:seed": "wrangler d1 execute workpass-cert-db --file=./database/seed.sql",
    "db:seed:local": "wrangler d1 execute workpass-cert-db --file=./database/seed.sql --local"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "d1",
    "r2",
    "work-permit",
    "certificate-manager"
  ],
  "author": "",
  "license": "MIT"
}
